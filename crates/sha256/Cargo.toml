[package]
name = "sha256"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "SHA-256 and BIP340 tagged hashes, as the BLAKE2b proof of work needs them"

[dependencies]

[lints]
workspace = true
